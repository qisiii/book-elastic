# 核心：所有 /elastic/* 的请求转发到 /elastic/index.html（SPA 路由解析）
/elastic/* /elastic/index.html 200

# 可选：补全末尾斜杠，避免 /elastic 访问 404
/elastic /elastic/ 301

# 可选：兜底 404 适配（指向自定义 404 页面）
/elastic/404.html /elastic/index.html 200
