# 数据输入：文档和索引

Elasticsearch是一个分布式文档存储。Elasticsearch用存储已经序列化为JSON的复杂数据结构文档来代替将数据按照行列的方式。当你有多个Elasticsearch集群节点时，存储文档是分布在集群上且能够被任意节点访问的。

当一个文档被存储时，它会被索引并且可以近乎实时（1秒内）地搜索。Elasticsearch用一种叫做倒排索引的数据结构来支持非常快的全文搜索。一个倒排索引会列出文档中的每个唯一的词，并标识每个词所存在的文档。




